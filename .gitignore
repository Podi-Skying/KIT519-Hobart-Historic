node_modules/
dist/
.tools/
.DS_Store
*.log
.vite/
coverage/
# v1 single-file prototype, kept locally for reference only
archive/

# Local secrets (API keys)
.env.local
.env.*.local

# A3 evaluation: signed consent forms, raw notes, recordings — never commit (public repo)
docs/a3/evaluation/private/
